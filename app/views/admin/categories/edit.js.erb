// Show the new form in the right box.
heading = "Edit '<%= raw @category.name %>'";
bodyContent = "<%= escape_javascript(render(:partial=>'admin/categories/form')) %>";
showLargeModal(heading, bodyContent);

// When the image upload form is submitted, the server response will appear in this iframe -->
var $div = $('<iframe id="iframe_category" name="iframe_category" width="100%" style="display:none"></iframe>').appendTo('body');