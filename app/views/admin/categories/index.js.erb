closeModal();
<% if @categories.any? %>
  // Fill the right box with first enquiry details in the list
  $('#div_category_index').html("<%= escape_javascript(render(:partial=>'admin/categories/index')) %>");
<% else %>
	var noResultsText = "<%= escape_javascript(theme_panel_message(I18n.translate('forms.no_results')))%>";
  $('#div_category_index').html(noResultsText);
<% end %>